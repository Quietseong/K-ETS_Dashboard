[project]
name = "k-ets-dashboard"
version = "1.0.0"
description = "K-ETS 탄소배출권 통합 관리 시스템"

[tool.pytest.ini_options]
testpaths = ["tests"]
markers = [
    "integration: 외부 서비스 연동 테스트",
    "slow: 실행 시간이 긴 테스트",
]
